[package]
name = "botserver"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Host occupant and per-bot actors"

[dependencies]
botserver-domain = { path = "../domain" }
serde_json = "1"
rusqlite = { version = "0.40.0", features = ["bundled"] }

[lints]
workspace = true
